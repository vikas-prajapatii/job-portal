server:
  port: 8888

spring:
  application:
    name: job-portal-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vikas-prajapatii/job-portal
          search-paths: backend/config-repo
          default-label: main
          clone-on-start: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh
  endpoint:
    health:
      show-details: always
